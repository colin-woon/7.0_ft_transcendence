# Technical Stack & Implementation Guidelines

## 1. Web Strategy (A)
* **Framework:** Next.js (App Router)+ TypeScript (Industry standard for SSR/BFF architecture; provides automatic routing and optimized data fetching.)
* **Build Tool:** Turbopack with SWC.
* **Package Manager:** npm (keeping it simple)
* **Styling:** Tailwind CSS (Rapid UI development).
* **Type Safety:** TypeScript (Strict Mode).
* **State Management Libraries:** Zustand (Client state: auth status, modals, form drafts)
* **HTTP Client:** native Next.js fetch() + wrapper in lib/apiClient.ts.
* **Pattern:** Atomic Design + Composition. Feature-Based Architecture with Colocation (AKA Domain Driven Design)
* **Formatting:** Biome (Single high-performance tool replacing ESLint and Prettier; keeps the team synchronized with zero-config overhead.)
* **Versions:** Next.js 15, Node v24, Tailwind v4

### Directory Structure (Next.js)
- When you need stores (Zustand):
  - Data shared across many components/pages (e.g., currentUser in authStore).
  - Client-only state survives navigation (modals open, theme, form drafts).
  - Avoid prop drilling/Context hell in larger apps.
```yaml
web/src
├── app/                          # THE ROUTING LAYER (Keep this thin)
│   ├── layout.tsx                # Global shell (Providers, Navbar)
│   ├── (auth)/                   # (<NAME>) used for Route Group: omitted by the url, purely for grouping
│   │   ├── login/        
│   │   │   └── page.tsx          # This URL will look like localhost:3000/login
│   │   └── profile/        
│   │       └── page.tsx
│   ├── (forum)/          
│   │   └── project/        
│   │       ├── page.tsx          # /project shows all projects      
│   │       └── [id]      
│   │           └── page.tsx      # Dynamic segment (/project/123)
│   ├── (chat)/           
│   │   └── messages/        
│   │       ├── page.tsx          # /shows all chats      
│   │       └── [id]
│   │           └── page.tsx      # Dynamic segment (/chat/123)
├── features/                     # FEATURE SLICED DESIGN
│   ├── auth/                     # Everything related to Auth Group
│   │   ├── ui/                   # LoginForm.tsx, UserAvatar.tsx
│   │   │   ├── login/  
│   │   │   │   ├── ContinueWithGoogleButton.tsx  
│   │   │   │   ├── LoginCard.tsx  
│   │   │   │   └── index.ts      # export { ContinueWithGoogleButton } from './ContinueWithGoogleButton'
│   │   │   ├── profile/ 
│   │   │   │   ├── ProfileCard.tsx  
│   │   │   │   ├── UserAvatar.tsx 
│   │   │   │   └── index.ts
│   │   │   └── index.ts          # export * from './login'; export * from './profile'
│   │   ├── model/                # Flow: Store holds raw data → Hook reads store + adds derived logic → Model validates/transforms data.
│   │   │   ├── store.ts          # Zustand: authStore (Persist data across components)  
│   │   │   ├── hooks.ts          # useAuthStore() (hooks into React features like state, effects, context, lives in components)
│   │   │   └── <model_name>.ts   # user.ts (Types + Validation Functions only)
│   │   ├── api/                  # Pass-thru to lib/apiClient.ts (Axios wrapper). Add auth headers, baseURL per service.
│   │   │   ├── auth.ts           # login(), logout(), getUser()
│   │   │   ├── profile.ts        # getProfile(), updateProfile()
│   │   │   └── index.ts          # Barrel exports: export * from './auth'
│   │   └── index.ts              # Feature root: export * from './model'; export * from './api'; export * from './ui'
│   ├── forum/                    
│   │   ├── ui/                   
│   │   │   ├── project/
│   │   │   │   ├── <component>.tsx
│   │   │   │   └── index.ts
│   │   │   └── index.ts
│   │   ├── model/                
│   │   │   ├── store.ts
│   │   │   ├── hooks.ts
│   │   │   └── <model_name>.ts
│   │   ├── api/          
│   │   │   ├── project.ts
│   │   │   └── index.ts
│   │   └── index.ts     
│   ├── chat/                     
│   │   ├── ui/
│   │   │   ├── messages/
│   │   │   │   ├── ChatRoom.tsx
│   │   │   │   └── index.ts
│   │   │   └── index.ts
│   │   ├── model/
│   │   │   ├── store.ts
│   │   │   ├── hooks.ts
│   │   │   └── <model_name>.ts
│   │   ├── api/          
│   │   │   ├── room.ts
│   │   │   ├── message.ts
│   │   │   └── index.ts
│   │   └── index.ts     
├── components/                   # SHARED UI (The Design System)
│   └── ui/                       # Shadcn/ui Atoms (Button, Input, Card)
├── lib/                          # SHARED UTILS (Third-party config)
│   ├── apiClient.ts              # Axios/Fetch wrapper
│   └── biome.json                # Linter config
└── hooks/                        # SHARED HOOKS (useDebounce, useMediaQuery)
```

#### Example contents in (lib/apiClient.ts)
```ts
// lib/apiClient.ts
const apiClient = (endpoint: string, options: RequestInit = {}) => 
  fetch(`/api/proxy${endpoint}`, {  // Proxy to your Java services
    credentials: 'include',  // Cookies for auth
    headers: { 'Content-Type': 'application/json' },
    ...options,
  }).then(res => {
    if (!res.ok) throw new Error(`HTTP ${res.status}`);
    return res.json();  // Auto JSON
  });

export const authApi = {
  login: (data: LoginInput) => apiClient('/auth/login', { method: 'POST', body: JSON.stringify(data) }),
};
```

#### Example contents in (auth/api/auth.ts)
```ts
// Calls your Java auth service
export async function login(credentials: LoginInput) {
  return apiClient.post('/auth/login', credentials);
}

export async function getCurrentUser() {
  return apiClient.get('/auth/me');
}
```

## 2. Data Communication Standards
**Serialization Format:** JSON.
**Naming Convention**: camelCase for all API keys.
**Standard Response Wrapper**: All APIs must return a consistent structure:
```JSON
{
  "status": "success",
  "data": { ... },
  "error": null
}
```
**Type Definition:** Types are colocated within `/features` folders; shared types reside in `/src/types`.


## 3. Backend: Auth Service (Auth)
* **Framework:** Quarkus (Container-first, fast startup).
* **Build Tool & Package Manager:** Maven, but managed by Quarkus CLI `quarkus extension add <package>`
* **Pattern:** Service-Repository + Anemic Domain Model.
* **Data Layer:** Hibernate ORM with Panache (Repository Pattern).
* **Validation:** Jakarta Bean Validation (Hibernate Validator).
* **Formatting:** Spotless Maven Plugin (Google Java Format).
* * **Versions:** Java 21.0.9-tem, Quarkus 3.27.1, Maven 3.9.11

### Directory Structure (Java)
```yaml
src/main/java/org/acme
├── /api           # Controllers (Strict Input/Output, HTTP only)
├── /service       # Business Logic (OAuth, Vault calls)
├── /model         # Entities (1:1 mapping with DB tables)
├── /dto           # Data Transfer Objects (Public structs)
└── /repository    # DB Access (Panache/SQL)
```

## 4. Backend: Chat Service
* **Framework:** net/http + chi (lightweight router for net/http)
* **Build Tool & Package Manager:** go mod (built-in)
* **Pattern:** Clean Architecture / Hexagonal / Contract-First design
* **Data Layer:** sqlc (generates Go code from SQL) + pgx/v5 (PostgreSQL driver)
* **Validation:** go-playground/validator/v10
* **Formatting:** goimports (gofmt + import management) + golangci-lint
* **Libraries:** gorilla/websocket (Explicit control over buffers) / oapi-codegen (OpenAPI codegen) / swagger-ui-dist
* **Versions:** Golang 1.25

### Directory Structure (Go)
```yaml
chat
├── api/
│   │── index.html            # static file for swagger ui interface
│   └── openapi.yaml          # OpenAPI Contract
├── cmd/
│   └── server/
│       └── main.go           # Entrypoint: init 
├── internal/                 # Code that cannot be imported by other projects
│   ├── api/                  # 
│   │   │── chat.gen.go       # Generated by oapi-codegen or gnostic from openapi.yaml
│   │   └── message.gen.go       # Generated by oapi-codegen or gnostic from openapi.yaml
│   ├── server/               # Business Logic HTTP-to-DB mapping MIDDLEMAN for api and database
│   │   │── routes.go         # "register" the generated handler.
│   │   ├── server.go         # The "Config" (The "Engine")
│   │   │── friendship.go     # HTTP-to-DB mapping for friendships
│   │   └── message.go        # HTTP-to-DB mapping for messages
│   └── database/             # Repository layer, DB Access (This is where sqlc generated code lives)
│       ├── database.go       # Database connection 
│       ├── models.go         # sqlc generated 
│       ├── friendship.sql.go # sqlc generated
│       └── message.sql.go    # sqlc generated
├── sql/
│   ├── schema.sql            # Table definitions
│   ├── queries.sql           # Raw SQL queries for sqlc to process
│   ├── friendship.sql           
│   └── message.sql           
├── .air.toml                 # Hot-reloading configuration
├── go.mod                    # Dependency tracking (Equivalent to pom.xml)
├── sqlc.yaml                 # sqlc configuration
└── Dockerfile                # The multi-stage Alpine build file
```

## 5. Backend: Python Service (Forum)
* **Framework:** FastAPI (High performance, async native).
* **Package Manager** uv `uv add <package>`
* **Pattern:** Procedural/Functional (C-style logic).
* **Type Safety:** Pyright (Static) + Pydantic (Runtime).
* **ORM:** SQLAlchemy (Standard relational mapper).
* **Formatting:** Ruff (Linting + Formatting).
* **Libraries:** uvicorn (ASGI web server in C)
* * **Versions:** Python 3.13, uv 0.9.22

### Directory Structure (Python)
```yaml
forum/
├── src/
│   ├── internal/        # Utils (JWT Auth, Markdown Sanitizer)
│   ├── database.py      # DB Connection
│   ├── logic.py         # Business Logic Functions
│   ├── main.py          # Entrypoint
│   ├── models.py        # SQLAlchemy Models
│   └── schemas.py       # Pydantic Models
└── tests/               # External tests
```

## 6. Infrastructure & Data
* **Database:** PostgreSQL (Relational Data).
* **Caching:** Redis (Session storage, WebSocket Pub/Sub, Online Status).
* **Secrets:** HashiCorp Vault.

## 7. Branching & Git Strategy
**Option 1 (everyone is a Full-Stack Dev, need to make sure the feature integrates with everything, point A-Z, u learn all stacks)**
```sh
feat/[name] (Standard features - spans multiple directories)
infra/[name] (Changes to Nginx, Postgres, Redis, or Docker)
docs/[name] (Markdown and architectural updates)
refactor/[name] (Code cleanup with no logic changes)
```

**Option 2 (each person has ownership of 1 stack, need to define the shared interface/types/api design very clearly, u specialize)**
```sh
frontend/[feature_name]
auth/[feature_name]
chat/[feature_name]
forum/[feature_name]
nginx/[feature_name]
postgres/[feature_name]
redis/[feature_name]
integration
```
* **PR Rules:** Must pass Spotless/Ruff check before merge.
